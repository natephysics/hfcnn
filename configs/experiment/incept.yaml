# @package _global_

# to execute this experiment run:
# python hfcnn experiment=overfit

defaults:
  - override /action: train
  - override /model: inceptnet
  - override /callback: checkpoint

trainer:
  max_epochs: 30