# @package _global_

# to execute this experiment run:
# python hfcnn experiment=overfit

defaults:
  - override /transforms: half_resolution
  - override /action: prepare_and_train

action:
  name: reduce_dim